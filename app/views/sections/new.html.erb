<% provide(:title, 'Add Section') %>
<h1>Add Section</h1>

<div class="row">
  <div class="form">
    <%= form_for(@section) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Create section", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>