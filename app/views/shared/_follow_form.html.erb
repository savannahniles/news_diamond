<% id = "follow_form_" + @feed.id.to_s %>
<span id="<%= id %>" class = "follow_form">
  <% if current_user.following?(@feed) %>
    <%= render 'shared/unfollow' %>
  <% else %>
    <%= render 'shared/follow' %>
  <% end %>
</span>