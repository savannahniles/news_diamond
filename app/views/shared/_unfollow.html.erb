<%= form_for(current_user.relationships.find_by(feed_id: @feed),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Remove from your Newspaper", class: "btn" %>
<% end %>