<%= render 'shared/error_messages', object: f.object %>

<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :description %>
<%= f.text_area :description %>

<%= f.label :url %>
<%= f.text_field :url %>

<%= f.label :image_src %>
<%= f.text_field :image_src %>

<%= f.label :section %>
<%= collection_select(:feed, :section_id, Section.all, :id, :name) %>